# yaml-language-server: $schema=abdicate-1.1.schema.json
version: "1.1"
kind: "Module"
friendlyName: stafftracker
requires:
  services:
    rooms:
      interface: rooms@web-service
    staff:
      interface: staff@web-service
    zipcodes:
      interface: zipcodes@web-service
provides:
  website:
    interface: website
    x-port: 8000