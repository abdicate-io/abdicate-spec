# yaml-language-server: $schema=abdicate-1.0.schema.json
version: "1.1"
kind: "Service"
friendlyName: staff
requires:
  databases:
    db:
      alias: database
      interface: mysql:5
provides:
  website:
    interface: staff@web-service
    x-port: 8000