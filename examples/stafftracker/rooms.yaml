# yaml-language-server: $schema=abdicate-1.0.schema.json
version: "1.1"
kind: "Module"
friendlyName: rooms
requires:
  databases:
    orm:
      alias: db
      interface: mysql:5
provides:
  service:
    interface: web-service
    x-port: 8000